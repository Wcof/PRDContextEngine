# PMSkill — Project Instructions (AtomCode)

## Code Style

- Follow existing patterns in the codebase
- Write tests for new functionality
- Preserve Chinese comments and terminology in existing code
- Keep SKILL.md frontmatter to `name`, `description`, `disable-model-invocation` only

## Project Conventions

- All skills follow the 9-section template: Purpose / Context / Instructions / Thinking Protocol / 关联增强 / 失败模式 / 禁止做什么 / 产出示例 / Further Reading
- Product path: `docs/pm-context/pm-context.md` is the single source of truth
- Risk markers: `[待确认]` / `[假设]` / `[冲突]` are the only allowed uncertainty markers
- Audit triple: `<依据集> → [工具] → [转换: <具体推导类型>] → <产出>`
- auto 模式每个节点产出必须是可独立序列化的结构化分片（JSON/Markdown），推理过程（CoT）不写入 PMContext
- 节点分片一经落盘即视为 Frozen，仅 /pm-conflict-resolver 可对其做差分修改
- PMContext 增量更新：pm-need 增量模式仅扫 `[待确认]` `[假设]` `[冲突]` 标记段重跑 collect/refine，合并必须内联调 /pm-conflict-resolver（差分修改唯一合法主体）；resolver 仅改有标记段，无标记段硬保不动。用户 `$ARGUMENTS` 显式指 `--update §8` / `--update GAP-01` = 定点增量
- Skill 可见性：User-facing 的 `metadata.internal` 不设或 false；Engine 的 `metadata.internal: true`。frontmatter 只允许 `name` / `description` / `disable-model-invocation` / `metadata.internal` 四字段
- 技术栈硬门执行细节：pm-sketch Step -0.5 扫 PMContext §8 命中前端框架声明清单即硬触发 Scaffold，`--simple` flag 此场景视为无效。pm-aiprd Instructions 承接 §8 转写「技术栈契约」段