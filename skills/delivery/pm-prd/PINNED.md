# PINNED — /pm-prd

> 绝不可被稀释的核心约束（供运行时置顶加载）。冗长示例见 references/。

1. 输入仅读**配置块声明的 PMContext 路径**（默认 `docs/pm-context/pm-context.md`，以 `## PMSkill` 块的 `产物目录` 项为准），不从对话臆造需求
2. 双形态产出：ai-prd.md（Agent 可执行）+ human-prd.md（评审友好）
3. 每条规则/验收必须可追溯到 PMContext 的某项事实或决策
4. PMContext 中 `[待确认]` 项不得在 PRD 中升格为事实
5. 产物落盘到 `docs/pm-context/prd/`
